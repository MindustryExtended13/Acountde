--vardef B = acountde

--cd dimension.%B%-
    overworld = Overworld
    test = Test dimension
--cde

--cd %B%.
    //setting categories
    settings.category = Acountde config

    //other
    clear-name = Clear name
    refresh = Reload
    out = Output
    you_in = You in:
    rename = Rename

    //money
    money = Money
    export-coin = Export coins
    import-coin = Import coins

    //other
    --cd unlock.
        blocks = Unlock all blocks
        items = Unlock all items
        liquids = Unlock all liquids
        units = Unlock all units
        all = Unlock all
    --cde

    --cd setting-split.
        betamindy-main = Betamindy main
        betamindy-asset = Betamindy content

        main = Main config
        tech = Tech config
    --cde

    --cd prefixes.
        developer = [purple] --- Developer content --- []
        campaign = [red] --- Campaign content --- []
        sandbox = [accent] --- Sandbox content --- []
        corruption = [#403287] --- Corruption content --- []
    --cde
--cde

--cd setting.%B%-
   --cd betamindy-hack.
        name = Betamindy hack [red](to apply all changes restart need)[]
        description = Shows things that were hidden from the eyes of non-modders
   --cde

   --cd auto-bundle.
        name = Auto bundle load [red](to apply changes restart need)[]
        description = Auto loads bundles for content names that don`t have bundle
   --cde

   --cd anucoin-support.
        name = Anucoin support [red](to apply changes restart need)[]
        description = Makes block that transform coins into anucoin to work
   --cde

   --cd autosave.
        name = Use %B% version of autosave
        description = If enabled new autosave system will save all dimensions&not recommended to use
   --cde

   --cd save-interval.
        name = Acountde autosave interval
        description = If %B% autosave enabled, every n ticks will be called %B% save
   --cde

   --cd schizophrenia.
        name = Schizophrenia mode
        description = Mindustry turns into schizophrenia
   --cde
--cde

--cd block.%B%-
    //dimension
    --cd magic-cube.
        name = Magic cube
        description = Cube that allows to adventure between all avalible dimensions
    --cde

    //corruption
    --cd toxic-tower.
        name = Toxic tower
        description = A biological tower that uses acid to defense. First turret of corruption
    --cde

    //environment
    deep-enzor.name = Deep enzor
    shallow-enzor.name = Enzor
    palm-tree.name = Palm

    //developer environment tiles
    test-floor.name = Test floor
    light-test-floor.name = Light test floor
    dark-test-floor.name = Dark test floor
    solid-floor.name = Solid floor
    solid-floor1.name = Solid floor (type 1)
    solid-floor2.name = Solid floor (type 2)
    solid-floor3.name = Solid floor (type 3)
    solid-floor4.name = Solid floor (type 4)
-cde